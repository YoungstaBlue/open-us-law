# Dependencies for the published ingestion scripts.
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
httpx>=0.27
duckdb>=0.10          # local query layer for scripts/local_kb/
curl_cffi>=0.7          # anti-bot fetch for some state portals
pymupdf>=1.24           # PDF parsing (fitz)
pdfplumber>=0.11        # PDF parsing (some regulation portals)
python-docx>=1.1        # DOCX sources (a few state regs)
playwright>=1.40        # JS-rendered sources (run: playwright install chromium)

# Added when the 50-state statute scrapers (state_scrapers/) land:
#   selenium

# For the 50-state statute scrapers (scripts/state_scrapers/):
pydantic>=2.0
tenacity>=8.0
structlog>=24.0          # structured logging in the bulk-ingester pipeline (vaquill_pipeline)
selenium>=4.0           # only the 11 Selenium states (+ chromedriver on PATH)
